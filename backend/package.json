{
  "name": "chess-backend",
  "version": "1.0.0",
  "description": "Real-time multiplayer chess game backend server",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && npm start",
    "dev:watch": "tsc -w & nodemon dist/index.js",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No linting configured\" && exit 0"
  },
  "dependencies": {
    "ws": "^8.14.2",
    "chess.js": "^1.0.0-beta.6"
  },
  "devDependencies": {
    "@types/ws": "^8.5.8",
    "typescript": "^5.2.2",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5"
  },
  "keywords": [
    "chess",
    "websocket",
    "game",
    "multiplayer",
    "real-time",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}